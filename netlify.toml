[build]
  command = "npm run build"
  publish = "dist"
  # functions = "src/funcs"  # Uncomment when you have functions to deploy

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  autoLaunch = false

# [[redirects]]
#   from = "/api/*"
#   to = "/.netlify/functions/:splat"
#   status = 200

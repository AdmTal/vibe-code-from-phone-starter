{
  "name": "vibe-starter",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:offline": "tsc && BUILD_OFFLINE=true vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
